name: Project frontend 2
description: Installs the project vuejs frontend
require:
  - .sensible/services/httpd
  - .sensible/runtimes/pnpm
  - .sensible/tools/git
check: echo "hej"
user: website
script: |
  # Git clone project
  # Symlink to /var/www/project.lan
  # pnpm install
  # pnpm build
