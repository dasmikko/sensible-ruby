packages:
  rpm: [btop]


tasks:
  - name: Inline task
    check: id -u shit
    script: notify-send "Create user"
    
  - .sensible/apps/project-frontend
  - .sensible/apps/project-frontend-2
  - .sensible/tasks/test
