packages:
  rpm: [btop]


tasks:
  - name: Check whoami
    user: sample
    check: whoami
    script: whoami

  - .sensible/apps/project-frontend
  - .sensible/apps/project-frontend-2
  - .sensible/tasks/test
